# Complete profile import: what was built, tested and measured

Commit `b6c8339` on `main`. Design source: `Copy of MedSpaFind platform design.zip` (desktop `BeautyFind Business Profile.dc.html`, `Mobile v2`, `handoff/Business Profile Mobile v2.md`, `02-data-model.md`). Sample arrays in the template scripts (prices, FAQs, staff, reviews, promises, `demo1` videos) were treated as fixtures only; none of that text ships.

## Built

| Area | Where | Notes |
|---|---|---|
| Coverage manifest | `src/lib/import/coverage.ts`, `docs/coverage-manifest.md` (generated by `npm run import:manifest`) | 21 template elements: implementation, data, source rule, fallback, CTA, test. Template coverage and evidence readiness measured separately. Profiles classified `ready`, `ready_with_disclosed_gaps`, `needs_owner_information`, `needs_review`; stored on the listing and the import record. |
| Schema (additive) | `prisma/migrations/20260926090000_complete_profile`, `20260926090100_unpriced_services` | `Treatment.priceAgorot` nullable, price types `range`, `package`, `free`, `on_request`, `priceMaxAgorot`, `priceNote`, `taxIncluded`, source fields. Branch and ImportPlace: team, videos, languages, establishedYear, teamSize, facebook/tiktok/youtube, attributes (tri-state with source), editorial, meta title/description, mediaProvenance, profileStatus, profileChecklist, socials, coverage, costs. Existing hidden zero-price imported services became `on_request`. |
| Stage 2 extraction | `src/lib/import/profileExtract.ts`, `siteExtract.ts`, `scripts/import/crawl.ts` | Team cards (name + role, biography under them), YouTube ids and channels, languages from explicit statements only, founding year from "since" wording, before/after candidates held for the owner. Progressive budget: 5 relevant pages, extended once to 12 while template fields are missing; bounded sitemap read; Hebrew labels including הצוות, סניפים, סרטונים. |
| Social verification | `src/lib/import/socials.ts` | Published only with a site backlink, a handle equal to the domain label, provider plus link hub, or an owner entry. Same-name provider accounts stay unverified in `socials`. |
| Prices | `siteExtract.ts`, `importPublish.ts`, `format.ts`, `Services.tsx` | Unknown price is null, never 0; shown as "המחיר לא פורסם" with "לקבלת מחיר ופרטים" (opens the contact flow with business, branch and service ids; unclaimed listings get the direct-contact panel that never claims a request was sent). Category "from" uses comparable amounts only. Imported prices are stored as published with tax status unknown; the page says so instead of "לא כולל מע״מ". Empty section keeps its anchor with "פירוט השירותים טרם עודכן". |
| Editorial | `src/lib/import/editorial.ts`, `scripts/import/editorialCall.ts`, `stages/editorial.ts` | One structured call per evidence packet (default `claude-sonnet-5`, `IMPORT_EDITORIAL_MODEL`), 450 to 550 words, 5 to 8 FAQs with a basis each, meta, service summaries, heading choice. Deterministic checks (numbers, people, dashes, first person, generic praise, booking claims, lengths), at most one repair call, cached by evidence hash + prompt version, reserved and settled against the run budget and a per-run cap. Short drafts are flagged `needsMoreInfo` with the missing evidence and never padded; owner text is never replaced. `IMPORT_EDITORIAL_MOCK=1` uses the deterministic template draft. |
| Media | `src/lib/server/importMedia.ts` | Provenance per copy (source, page, provider, time, dimensions, bytes, sha256, alt, reuse basis, status), duplicate drop by hash, WebP derivatives (banner 1600px about 350KB, gallery 1200px about 160KB) with private originals, landscape banner, Hebrew alt from the source when descriptive. No photo: neutral monogram hero and `hero_media_missing` in admin. |
| Videos | `src/lib/import/youtube.ts`, `VideoEmbed.tsx` | Data API (`videos.list` per 50 ids, channel uploads) when `YOUTUBE_API_KEY` is set, else oEmbed. Only public embeddable videos; consent-gated click-to-load `youtube-nocookie` players; unavailable videos keep a working channel link. Quota counted per run. |
| Map | `src/lib/mapsEmbed.ts`, `MapEmbed.tsx`, `EmbedGate.tsx` | Maps Embed API place mode (`place_id:` or name + address, `language=he`, `region=IL`), behind the "תוכן מוטמע" consent with an activate control; Waze and Google directions with the place id. Without `NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY` the schematic map and links remain. |
| Profile page | `src/app/[region]/biz/[slug]/page.tsx`, `data.ts` | Every section kept with an honest state: about (heading from the editorial draft), services, reviews (no weekly-refresh claim), before/after, team (verified staff plus site team, labelled), videos, hours (per-day unknown, no open-now without hours), FAQ, location, contact (אימייל לא פורסם, verified socials only), three fact cards with sourced alternates and "פרטים טרם עודכנו", tri-state chips, mobile tabs and bar with neutral CTA copy. JSON-LD: offers without unknown prices, sameAs for verified accounts only, `foundingDate` when sourced, no Google aggregate rating. |
| Admin | `ops/import/review/Checklist.tsx`, `ReviewList.tsx`, `RunsView.tsx`, `actions.ts` | Per-record checklist: coverage rows, status, owner-only gaps, conflicts, draft (words, FAQs, model, repairs, violations, cost, text), team, languages, year, videos, socials with verification path, media and before/after pending, crawl budget, map status, per-record costs, retries; regenerate action (enhance run with `regenerate`). Run cards show editorial calls, cache hits, budget stops and YouTube quota; the estimator has editorial, YouTube quota and storage columns. New settings: editorial, YouTube, derivatives, map, extended page budget. |
| Worker | `scripts/import/work.ts`, `stages/enhance.ts`, `.github/workflows/import.yml` | Import runs: discover, enrich, posts, extraction, editorial, check. Enhance runs: refresh, posts, enrich, editorial, fill. New env: `IMPORT_EDITORIAL_MODEL`, `YOUTUBE_API_KEY`. |

## Tested

- `npm run test:import`: 99 tests pass (unit, database, and the new `tests/import/profile.test.ts` covering acceptance points 1 to 8 and the SSRF-safe fetch path). Mocked providers only.
- Simulated 10-business pilot (`npm run import:simulate`, report in `docs/import-pilot-SIMULATED.md`): rich clinic sites, a two-branch chain, a site without prices, sparse listings without a site. Rich profiles: 11 services (3 without a published price, 0 zero-priced), 8 copied photos, 2 playable videos, 3 team members from the site, 0 staff logins created, 8 FAQs, readiness 82%. Sparse: `needs_owner_information`, short draft flagged. Descriptions came from the deterministic template (mock); with the API the Sonnet call produces the 450 to 550 words.
- Production build and local `next start`: the listing renders with all sections; JSON-LD checked (11 offers, 8 with a price specification, verified sameAs, FAQ page, no aggregate rating from Google). Screenshots at 390, 768 and 1440px (full page, lightbox, contact flow, mobile sticky bar) were taken locally from the simulated data with `npm run import:screenshots`; they are not committed.
- Not performed: any live API call (DataForSEO, Anthropic, YouTube, Google Maps). No real scrape happened and no money was spent.

## Costs (reference rates, gross)

Per fully enriched profile: DataForSEO about $0.0004 (discovery share), editorial about $0.06 to $0.075 (Sonnet, one repair in four), YouTube 0 to 3 quota units (no charge), Maps Embed no charge, storage about 3 MB (six WebP derivatives plus originals). The discovery price alone is never reported as the profile cost. Estimator rows (defaults): 100 profiles about $3.9, 1,000 about $39, 10,000 about $390, dominated by editorial writing; Haiku 4.5 lowers that several times.

## Missing configuration

- `ANTHROPIC_API_KEY` (GitHub secret): without it the editorial stage stops with `no_api_key` and listings keep their source text.
- `NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY` (Vercel): browser key restricted to our domains and the Maps Embed API.
- `YOUTUBE_API_KEY` (GitHub secret, optional): channel discovery and exact embeddability; oEmbed covers videos found on the site without it.
- `BLOB_READ_WRITE_TOKEN` (GitHub secret, optional): lets the worker copy images during enhance runs; the admin copy button covers it otherwise.

## Genuine data gaps

- Before/after images are never imported; they wait for the owner's consent record.
- Team size, languages and founding year appear only when a source states them.
- Verified credentials and the מאומת badge come only from our verification workflow.
- Reviews from Google are a summary and a link; review texts are not copied.
